export default function Placeholder({ titulo }: { titulo: string }) {
  return (
    <div>
      <h1 className="text-2xl font-semibold">{titulo}</h1>
      <p className="mt-2 text-neutral-600">Pendiente de construir.</p>
    </div>
  );
}
